import { useEffect, useRef, useState } from "react";
import styles from "./Work.module.css";

const Work = () => {
    return (
        <div style={{ zIndex: 0}}>

        </div>
    );
};

export default Work;